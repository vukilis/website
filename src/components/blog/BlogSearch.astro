---
import '../../styles/blog/blog-search.css';

interface Props {
  allPosts: { id: string; data: { title: string; description?: string; tags?: string[]; categories?: string[] } }[];
}

const { allPosts } = Astro.props;

const globalPostIndex = allPosts.map(post => ({
  id: post.id,
  title: post.data.title,
  description: post.data.description ?? '',
  url: `/blog/${post.id}/`,
  tags: post.data.tags ?? [],
  categories: post.data.categories ?? [],
}));
---

<div class="blog-search-wrapper" id="blog-search-wrapper">
  <div class="blog-search" id="blog-search">
    <div class="search-icon">
      <svg xmlns="http://www.w3.org/2000/svg" width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="11" cy="11" r="8" />
        <line x1="21" x2="16.65" y1="21" y2="16.65" />
      </svg>
    </div>
    <input
      type="text"
      id="blog-search-input"
      class="search-input"
      placeholder="Search posts..."
      autocomplete="off"
      role="combobox"
      aria-autocomplete="list"
      aria-expanded="false"
      aria-controls="blog-search-suggestions"
      aria-label="Search blog posts by title and description"
    />
    <button
      id="blog-search-clear"
      class="search-clear"
      aria-label="Clear search"
      type="button"
      hidden
    >
      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" x2="6" y1="6" y2="18" />
        <line x1="6" x2="18" y1="6" y2="18" />
      </svg>
    </button>
    <span class="search-shortcut" aria-hidden="true">
      <kbd>⌘</kbd><kbd>K</kbd>
    </span>
    <ul
      id="blog-search-suggestions"
      class="search-suggestions"
      role="listbox"
      hidden
    ></ul>
  </div>
</div>

<script type="application/json" id="blog-search-posts-data" set:html={JSON.stringify(globalPostIndex)}></script>

<script is:inline>
(function() {
  function scheduleInit(id) {
    var key = '__blog_search_initialized_' + id;
    if (window[key]) window[key] = false;
    var wrapper = function () { return document.getElementById('blog-search-wrapper'); };
    var init = function () {
      var root = wrapper();
      if (!root) return;

      var input = root.querySelector('.search-input');
      var clearBtn = root.querySelector('.search-clear');
      var suggestionsList = root.querySelector('.search-suggestions');

      if (!input || !clearBtn || !suggestionsList) return;

      var dataEl = document.getElementById('blog-search-posts-data');
      var ALL_POSTS = [];
      if (dataEl) {
        try { ALL_POSTS = JSON.parse(dataEl.textContent || '[]'); } catch (e) { ALL_POSTS = []; }
      }

      var postMap = new Map();
      var postTitles = [];
      ALL_POSTS.forEach(function (post) {
        postMap.set(post.title.toLowerCase(), post);
        postTitles.push(post.title);
      });

      var currentQuery = '';
      var activeIndex = -1;
      var currentSuggestions = [];

      function highlightMatch(text, query) {
        if (!query) return text;
        var escaped = query.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
        var regex = new RegExp('(' + escaped + ')', 'gi');
        return text.replace(regex, '<mark class="search-highlight">$1</mark>');
      }

      function createSuggestionItem(title, description, query, index) {
        var li = document.createElement('li');
        li.className = 'search-suggestion';
        li.setAttribute('role', 'option');
        li.setAttribute('aria-selected', 'false');

        var textWrapper = document.createElement('span');
        textWrapper.className = 'suggestion-text';

        var titleSpan = document.createElement('span');
        titleSpan.className = 'suggestion-title';
        titleSpan.innerHTML = highlightMatch(title, query);

        textWrapper.appendChild(titleSpan);

        if (description) {
          var descSpan = document.createElement('span');
          descSpan.className = 'suggestion-description';
          descSpan.innerHTML = highlightMatch(description, query);
          textWrapper.appendChild(descSpan);
        }

        li.appendChild(textWrapper);

        var arrow = document.createElement('span');
        arrow.className = 'suggestion-arrow';
        arrow.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>';

        li.appendChild(arrow);

        li.addEventListener('click', function () {
          selectSuggestion(title);
        });
        li.addEventListener('mouseenter', function () {
          setActiveSuggestion(index);
        });

        return li;
      }

      function renderSuggestions(query) {
        currentSuggestions = [];
        suggestionsList.innerHTML = '';

        if (!query.trim()) {
          suggestionsList.hidden = true;
          input.setAttribute('aria-expanded', 'false');
          return;
        }

        var lowerQuery = query.toLowerCase();
        var matches = ALL_POSTS
          .map(function (post, idx) { return { title: post.title, description: post.description || '', url: post.url, idx: idx }; })
          .filter(function (item) { return item.title.toLowerCase().indexOf(lowerQuery) !== -1 || item.description.toLowerCase().indexOf(lowerQuery) !== -1; })
          .slice(0, 8);

        if (matches.length === 0) {
          var emptyLi = document.createElement('li');
          emptyLi.className = 'search-suggestion empty';
          emptyLi.setAttribute('role', 'option');
          emptyLi.setAttribute('aria-selected', 'false');
          emptyLi.innerHTML = '<span class="suggestion-empty-text">No posts match "<strong>' + query.replace(/"/g, '') + '</strong>"</span>';
          suggestionsList.appendChild(emptyLi);
          suggestionsList.hidden = false;
          input.setAttribute('aria-expanded', 'true');
          currentSuggestions = [];
          activeIndex = -1;
          return;
        }

        var headerLi = document.createElement('li');
        headerLi.className = 'suggestions-header';
        headerLi.setAttribute('aria-hidden', 'true');
        headerLi.textContent = matches.length + ' suggestion' + (matches.length > 1 ? 's' : '') + ' found';
        suggestionsList.appendChild(headerLi);

        var divider = document.createElement('li');
        divider.className = 'suggestions-divider';
        divider.setAttribute('aria-hidden', 'true');
        suggestionsList.appendChild(divider);

        matches.forEach(function (item, i) {
          var li = createSuggestionItem(item.title, item.description, query, i);
          suggestionsList.appendChild(li);
        });

        suggestionsList.hidden = false;
        input.setAttribute('aria-expanded', 'true');
        currentSuggestions = matches;
        activeIndex = 0;
        setActiveSuggestion(0);
      }

      function setActiveSuggestion(index) {
        activeIndex = index;
        var items = suggestionsList.querySelectorAll('.search-suggestion:not(.empty)');
        items.forEach(function (item, i) {
          var isActive = i === index;
          item.setAttribute('aria-selected', String(isActive));
          item.classList.toggle('active', isActive);
        });

        if (items[index]) {
          items[index].scrollIntoView({ block: 'nearest' });
        }
      }

      function selectSuggestion(title) {
        var entry = postMap.get(title.toLowerCase());
        if (!entry) return;
        input.value = entry.title;
        currentQuery = entry.title;
        clearBtn.hidden = false;
        input.classList.add('has-value');
        hideSuggestions();
        window.location.href = entry.url;
      }

      function hideSuggestions() {
        suggestionsList.hidden = true;
        suggestionsList.innerHTML = '';
        input.setAttribute('aria-expanded', 'false');
        currentSuggestions = [];
        activeIndex = -1;
      }

      input.addEventListener('input', function () {
        currentQuery = input.value;
        clearBtn.hidden = currentQuery === '';
        if (currentQuery.trim() !== '') {
          input.classList.add('has-value');
        } else {
          input.classList.remove('has-value');
        }
        renderSuggestions(currentQuery);
      });

      input.addEventListener('keydown', function (e) {
        if (!suggestionsList.hidden && currentSuggestions.length > 0) {
          if (e.key === 'ArrowDown') {
            e.preventDefault();
            var next = activeIndex + 1 < currentSuggestions.length ? activeIndex + 1 : 0;
            setActiveSuggestion(next);
            input.value = currentSuggestions[next].title;
            currentQuery = currentSuggestions[next].title;
            return;
          }
          if (e.key === 'ArrowUp') {
            e.preventDefault();
            var prev = activeIndex - 1 >= 0 ? activeIndex - 1 : currentSuggestions.length - 1;
            setActiveSuggestion(prev);
            input.value = currentSuggestions[prev].title;
            currentQuery = currentSuggestions[prev].title;
            return;
          }
          if (e.key === 'Enter') {
            e.preventDefault();
            if (activeIndex >= 0 && activeIndex < currentSuggestions.length) {
              selectSuggestion(currentSuggestions[activeIndex].title);
            } else {
              hideSuggestions();
            }
            return;
          }
          if (e.key === 'Escape') {
            hideSuggestions();
            return;
          }
        }

        if (e.key === 'Escape') {
          hideSuggestions();
        }
      });

      input.addEventListener('focus', function () {
        if (currentQuery.trim()) {
          renderSuggestions(currentQuery);
        }
      });

      input.addEventListener('blur', function () {
        setTimeout(function () {
          hideSuggestions();
        }, 180);
      });

      clearBtn.addEventListener('click', function () {
        input.value = '';
        currentQuery = '';
        clearBtn.hidden = true;
        input.classList.remove('has-value');
        hideSuggestions();
        input.focus();
      });
    };

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init, { once: true });
    } else {
      init();
    }
  }

  scheduleInit('blog-search');
  document.addEventListener('astro:page-load', function () { scheduleInit('blog-search'); });

  function focusSearchInput() {
    var input = document.getElementById('blog-search-input');
    if (input) input.focus();
  }

  document.addEventListener('keydown', function (e) {
    if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
      e.preventDefault();
      focusSearchInput();
    }
  });
})();
</script>